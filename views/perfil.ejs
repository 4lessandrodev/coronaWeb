<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/dashboard.css">
    <title>Meu Perfil</title>
</head>
<!-- rota para acessar essa view:
    http://localhost:3000/users/perfil -->

<body>
    <header>
        <nav class="container-fluid">
            <h1>
                <%= modelo.nome %>
            </h1>
        </nav>
    </header>
    <main>
        <h2 id="h-dados">Dados do usuário</h2>

        <form class="dados" action="">
            <div class="form-group">
                <label for="nome">Nome</label>
                <input value="<%= modelo.nome %>" readonly type="text ">
            </div>
            <div class="form-group ">
                <label for="idade ">Idade</label>
                <input value="<%= modelo.idade %>" readonly type="text ">
            </div>
            <div class="form-group ">
                <label for="sexo ">Sexo</label>
                <input value="<%= modelo.genero %>" readonly type="text ">
            </div>
            <div class="form-group ">
                <label for="telefone ">Telefone</label>
                <input value="<%= modelo.telefone  %>" readonly type="tel ">
            </div>
            <div class="form-group ">
                <label for="endereco ">Endereço</label>
                <input value="<%= modelo.cep %>" readonly type="text ">
                <input value="<%=  modelo.endereco %>" readonly type="text ">
            </div>
            <div class="form-group ">
                <label for="email ">E-mail</label>
                <input value="<%= modelo.email %>" readonly type="mail ">
            </div>
            <div class="form-group ">
                <button type="button ">Editar Perfil</button>
            </div>
        </form>
    </main>
</body>

</html>